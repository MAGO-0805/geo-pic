# 功能开关配置文件
# use_path_tracing: true=路径追踪, false=Whitted光线追踪
# direct_lighting: mis=BRDF+NEE多重重要性采样, brdf=纯BRDF采样, nee=纯NEE直接光照
# use_omp: true=OpenMP并行加速, false=单线程

use_path_tracing = true
direct_lighting = nee
use_omp = true
